# Espafloria Master Context — standing instructions

База знаний проекта автоматизации Espafloria SL (Odoo + Make.com + Telegram bot).
**Owner:** Andriy  
**Prod:** espafloriasl.odoo.com  
**Target:** 1 января 2027 — полный cutover с Holded

---

## 📋 Читай при старте задачи (в таком порядке)

1. `master-context/00_master_index.md` — карта файлов + глоссарий
2. `master-context/99_invariants.md` — **всегда** перед любой правкой логики  
3. Файлы по теме задачи (`01_business_context.md` → `11_crm_and_customers.md`)

---

## 🚨 Жёсткие правила (non-negotiable)

- **`99_invariants.md` редактируется только с явным подтверждением в чате**
- **Бизнес-файлы (01-11) — факты, не история.** Не добавляй записи «мы сделали X», правь прямо в нужном месте как будто так и было
- **Snapshot-артефакты** (`*.py`, `prompt_*.txt`, `make_line_log_*.txt`) — синкай с prod при любой правке prod-копии  
- **Перед `git push` всегда показывай diff и жди ок**

---

## 📝 Версионирование .md

Каждый `.md` начинается с `<!-- v: N | updated: YYYY-MM-DDTHH:MMZ -->`.  

**При значимой правке:**  
1. bump `v` в header файла  
2. обнови строку в `VERSIONS.md`  
3. bump самого `VERSIONS.md`  

**Опечатки/пунктуация** — только `updated` (без bump `v`).

---

## 🗂️ Коммиты

- **CHANGELOG.md:** одна строка сверху на сессию, формат `- YYYY-MM-DD — <subject>`. Больше 15 строк — удали самую старую.
- **Commit message:** subject первой строкой, пустая строка, внятный body.

---

## ❌ Чего не делать

- Не создавай новых `.md` без моего одобрения
- Не трогай `legacy_migrations/` (одноразовые скрипты)  
- Не рефактори ради рефакторинга — меньше правок лучше
- Не меняй архитектуру flat layout'а

---

## 🎯 Конечная цель

**Умная сеть цветочных магазинов** — база и роботы делают основную работу по контролю сотрудников и структурной целостности бизнеса. Детали в `10_vision_and_roadmap.md`.

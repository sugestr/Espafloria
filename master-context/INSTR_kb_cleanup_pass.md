<!-- v: 1 | updated: 2026-05-02T22:50Z -->

# INSTR — KB cleanup pass (компактификация и логичность базы знаний)

Промт для отдельного чата. Самодостаточный — копировать целиком.

Контекст использования:
- **Триггер:** база знаний разрослась после нескольких циклов работы (reset, re-imports, новые INSTR, audit reports). Мы хотим компактнее, понятнее, без противоречий.
- **Когда запускать:** периодически (раз в 2-3 недели интенсивной работы), либо когда owner чувствует «расползание».
- **Не для:** добавления нового контента. Только cleanup существующего.

---

## Промт начинается

Ты работаешь над Espafloria SL Odoo 19 SaaS Custom (`espafloriasl.odoo.com`). Цель этой сессии — **сделать базу знаний (`master-context/`) компактнее, понятнее, без противоречий**, **не теряя смысла**.

### Что прочитать в самом начале

1. `/Users/andriy/Documents/master-context/CLAUDE.md` — правила работы AI/инженера. Особенно секция «`master-context/` = только база знаний».
2. `/Users/andriy/Documents/master-context/master-context/00_index.md` — карта файлов. Используй её как master-list что есть.
3. `/Users/andriy/Documents/master-context/master-context/99_invariants.md` — 5 инвариантов. Не нарушать.
4. `/Users/andriy/Documents/master-context/master-context/CHANGELOG.md` — последние 15 записей. Поможет понять что недавно меняли.

### Принципы owner (verbatim)

- «Логичность, компактность, непротиворечивость базы знаний»
- «Чтобы оно все не расползалось на кучу непонятных деталей»
- «Бизнес-файлы (01-08) — факты, не история»
- «Не добавляй записи "мы сделали X". Правь прямо в нужном месте, как будто так и было. История — в CHANGELOG.md»

### Workflow

#### Шаг 1 — inventory

Перечисли все `.md` в `master-context/` (не `memory/`):

```bash
ls master-context/*.md master-context/*.py master-context/*.txt
```

Для каждого файла зафиксируй:
- размер (в строках)
- header version + updated date
- последний commit с git log (когда правился)

#### Шаг 2 — найти проблемы

Пройдись по каждому файлу и найди (без правок пока):

**(a) Противоречия:**
- Один и тот же факт описан в 2+ местах с разными значениями
- Cross-references на удалённые/переименованные файлы
- Status (🟢/🟡/🔴) не совпадает с реальностью (например, файл говорит «PROD», а после reset это уже READY)

**(b) Дубли:**
- Текст-копипаст между файлами (одна формулировка в 2-3 местах)
- Глоссарий-термин определён дважды

**(c) Stale (устаревшее):**
- Конкретные цифры из прошлой работы (например, «138 pedido закрыто», «id=42 Verdnatura») — после reset они мертвы
- Дедлайны прошедшие («до 20 апреля»)
- Упоминания удалённых cards/sessions/orders
- Ссылки на закрытые/удалённые ID

**(d) Расползание:**
- Длинная история «мы сделали X» вместо короткого «X есть»
- Лишние meta-комментарии («раньше было Y, теперь Z» — если Z давно стабильно, Y можно удалить)
- Bullet списки которые могли быть таблицей или одной строкой

**(e) Битые ссылки:**
- `[link](file.md)` где file.md удалён/переименован
- Пути типа `master-context/foo` где foo переехал

#### Шаг 3 — DRY-RUN отчёт

**ДО любых правок** покажи owner:

```
=== KB CLEANUP DRY-RUN REPORT ===

Inventory: X файлов / Y строк / Z артефактов

Проблем найдено:
  Противоречий: N (примеры)
  Дублей: M (примеры)
  Stale facts: K (примеры с цитатами)
  Расползания: P (примеры)
  Битых ссылок: Q

Предлагаемые правки (по файлам):
  master-context/01_project.md:
    - Удалить: «...» (stale)
    - Сократить: «...» (расползание)
    - Поправить: «...» → «...» (противоречие с 04_pos)
  master-context/02_makecom_bot.md:
    - ...
  ...

Файлы которые НЕ трогаю (стабильные):
  - 99_invariants.md (только инварианты, не cleanup-зона)
  - reception_audit_v12_prompt.md (исторический prompt)
  - reception_audit_v14_prompt.md (исторический prompt)
  ...

Estimated total строк удалено: ~N
Estimated total строк перефразировано: ~M
```

**ЖДИ owner «ок»** на план. Не приступай к правкам.

#### Шаг 4 — правки batch (после ок)

Применяй правки **по одному файлу за раз**. После каждого:
- Show diff stat (`git diff --stat <file>`)
- Bump `v: N` в header если правка значимая (или только `updated:` если опечатки)
- Move on к следующему

**Не переименовывай файлы** в этом cleanup проходе — это отдельная reorganization. Только содержимое.

**Не удаляй файлы** без явного «удалить» от owner per-file.

#### Шаг 5 — special checks

**(a) 02_makecom_bot.md** — owner подтвердил «конечно вернётся, не трогаем». Это значит:
- НЕ ставить DEPRECATED-шапку
- НЕ удалять
- Можно поправить stale ссылки/cross-references если появились

**(b) reception_handover_2026-04-29.md** — это **v1 baseline алгоритма реконсиляции**. Сохранять. Не помечать как устаревший. Можно дополнить статус-маркером в шапке если ещё нет (но он уже есть после 2026-05-02).

**(c) reception_algorithm.md** — current spec, актуален. Trogat только если найдено внутреннее противоречие.

**(d) reception_audit_v12/v14_prompt.md** — это исторические audit prompts. Снова применимы для будущих v20+ аудитов. **Не сокращать**, не удалять. Можно уточнить cross-references если они оборвались.

**(e) memory/MEMORY.md и memory/*.md** — это auto-memory Claude. **Не трогать руками**. Owner сам управляет (просит сохранять/удалять). Этот файл вне cleanup scope.

#### Шаг 6 — verify

После всех правок:
- `git diff --stat` (общая картина)
- `grep` на конкретные кейсы которые ты находил в шаге 2 — все ушли?
- Все cross-references в 00_index.md ведут на существующие файлы?

#### Шаг 7 — финальный отчёт owner

```
=== KB CLEANUP DONE ===

Файлов изменено: N
Строк удалено: K
Строк перефразировано: M
Файлов без изменений: P

Главные находки:
  - {concrete examples}

Что осталось на потом (не сделал в этой сессии):
  - {если что-то нашёл что требует решения owner}
```

#### Шаг 8 — CHANGELOG + commit

Одна строка в `CHANGELOG.md` сверху (bump v):

```
- 2026-05-XX — **KB cleanup pass**: N файлов почищено (убрано K строк stale + M противоречий + P дублей). Не тронуты: 02_makecom (will return), 99_invariants, reception_audit_*, memory/. Подробности по файлам — git diff коммита.
```

Git commit + push (через `mcp__Desktop_Commander__start_process`).

### Hard rules (CLAUDE.md)

- **Не создавай новых .md** без явного approval owner.
- **Не рефактори ради рефакторинга** — меньше правок лучше. Cleanup, не переписывание.
- **Не меняй архитектуру flat layout** (все артефакты на одном уровне с .md).
- **Не утверждай поведение Odoo** из памяти — если правишь stale fact, проверь live-базой через MCP.
- **author_id=56** не нужен (это про Odoo chatter, здесь только репо).
- **`99_invariants.md` НЕ трогать** без явного chat-разрешения.
- **memory/ НЕ трогать** руками — это auto-memory.

### Запрещено

- НЕ объединять `.md` файлы (если 2 файла можно слить — это отдельное reorganization, не cleanup).
- НЕ переименовывать файлы.
- НЕ удалять `.md` файлы.
- НЕ менять `00_index.md` структуру (только обновить ссылки если что-то поломалось).
- НЕ трогать `02_makecom_bot.md` контентно (только если найдена битая ссылка).
- НЕ трогать `reception_handover_2026-04-29.md` контентно (это v1 baseline, протекций нет).

### Что МОЖНО

- Удалять stale facts (с подтверждением owner на dry-run).
- Перефразировать длинные параграфы в более короткие без потери смысла.
- Обновлять status-маркеры (🟢/🟡/🔴) если они не совпадают с реальностью.
- Чинить cross-references на переименованные файлы.
- Свернуть bullet-списки в таблицы где это уместно.
- Удалять расползающиеся meta-комментарии («раньше было X, теперь Y» если Y давно стабильно).

## Промт заканчивается

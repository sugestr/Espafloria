Fully flat layout: collapse artifacts/ tree, one subfolder remains

Owner flagged that the structure was still too nested: prompts/,
templates/, code/odoo_actions/, code/migrations/, scripts/ all lived
under artifacts/, which made the "upload to Project knowledge" step
still require navigating several subfolders. Flattening further so
that everything that gets uploaded lives at master-context/ root,
with a single subfolder for the legacy one-off scripts.

Layout changes (9 git mv)
- master-context/artifacts/prompts/*.txt       → master-context/  (3 files)
- master-context/artifacts/templates/*.txt     → master-context/  (2 files)
- master-context/artifacts/code/odoo_actions/*.py → master-context/  (3 files)
- master-context/artifacts/scripts/*.sh        → master-context/  (1 file)
- master-context/artifacts/code/migrations/    → master-context/legacy_migrations/  (3 files, folder rename)
- artifacts/ folder tree removed entirely (no empty dirs left)

Relative links in py files restored to plain siblings
- calculate_in_shop_action.py: ../../../03_odoo_receipt_review.md → 03_odoo_receipt_review.md
- migrate_variant_action.py:   ../../../06_catalog_migration_toolkit.md → 06_catalog_migration_toolkit.md

Documentation reflects flat layout
- README.md v4→v5: structure diagram + ultra-simple upload checklist
- VERSIONS.md v7→v8: artifacts listed by name (no subfolders)
- SYNC_STATE.md v6→v7: upload step reduced to "⌘A then unselect legacy_migrations/"
- CHANGELOG.md v8→v9: new rolling entry
- 12_ai_workflow.md v5→v6: "Состав базы" section rewritten, script path updated
- 00_master_index.md v7→v8: Artifacts tables simplified
- 00_source_files_index.md v4→v5: sections reorganized around flat file groups

Commit script path updated
- Was: bash ~/Documents/master-context/master-context/artifacts/scripts/commit_worker_delivery.sh
- Now: bash ~/Documents/master-context/master-context/commit_worker_delivery.sh

Script internals unchanged — it never referenced its own location.

Not touched: 01-11, 99_invariants — business content unchanged.

Owner upload after this commit
1. Clear Project knowledge
2. Finder → ~/Documents/master-context/master-context/
3. ⌘A
4. ⌘-click on legacy_migrations/ folder to unselect it
5. Drag-drop to Project knowledge

One action. Fully flat.

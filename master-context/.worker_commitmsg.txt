Move README into master-context/, leave GitHub stub at root

Owner flagged that uploading Project knowledge still required two drag-drop
actions: one for the master-context/ folder and one for the root-level
README.md. The point of the flat layout is that everything gets uploaded
in a single action.

Solution: move the full README into master-context/ along with everything
else. Leave only a minimal 3-line stub at the repo root so GitHub's
landing page still shows a description and a pointer to the real base.

Files touched
- README.md (repo root): reduced to stub (name + pointer to master-context/README.md)
- master-context/README.md (new; formally the "moved" version): full
  content of the former root README, adjusted for its new location
  (sibling paths to other .md files, note about the root stub)
- master-context/VERSIONS.md v8→v9: README row moved into main table
  (no longer marked "repo root"); footnote about the stub
- master-context/SYNC_STATE.md v7→v8: clarifies that full README is now
  part of the single drag-drop, root stub stays in git only
- master-context/CHANGELOG.md v9→v10: new rolling entry at top
- master-context/12_ai_workflow.md v6→v7:
  - "Состав базы" section: root is now only the stub, full README lives
    in master-context/
  - "См. также" link updated from ../README.md to README.md (sibling)
  - Reviewer briefing no longer lists "README + master-context/*.md" —
    all 19 .md live in master-context/ now

Side benefit: the full README now gets loaded into Project knowledge on
every upload, so new worker chats see the repo rules at session start
without needing an extra upload step.

Not touched: 01-11, 99_invariants, 00_master_index, 00_source_files_index —
nothing about the README move affects business content or navigation.

Owner upload after this commit
Same as before, single action:
1. Clear Project knowledge
2. Finder → ~/Documents/master-context/master-context/
3. ⌘A
4. ⌘-click legacy_migrations/ to unselect
5. Drag-drop

The full README now lands in Project knowledge along with everything else.

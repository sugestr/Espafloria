Two transport paths: Claude Code + sandbox-delivery zip, both valid

После установки Claude Code появился альтернативный путь доставки правок
помимо zip/sandbox-delivery через web-чат. Фиксируем в базе что **оба пути
валидны**, выбор — по контексту Owner'а.

## Изменения

### 12_ai_workflow.md v8 → v9 (главное)
- Новая секция «🚚 Два пути доставки правок» (сразу после § Роли):
  * Path A — Claude Code в терминале на Mac (CLAUDE.md читается автоматически)
  * Path B — sandbox-delivery через zip + commit_worker_delivery.sh
  * Таблица «Выбор за 10 секунд»
  * Явно: правила версионирования/CHANGELOG/bump одинаковы для обоих путей
- § «Три стадии работы worker'а» теперь с дисклеймером что это детальный
  протокол Path B; в Path A большинство шагов автоматизировано инструментом

### SYNC_STATE.md v9 → v10
- § Upload в Project knowledge структурирован в два подраздела:
  * «Рекомендуемый путь — GitHub connector» (primary)
  * «Fallback — ручной drag-drop» (старая процедура как fallback)
- versions_md_current → 11, last_session обновлён

### README.md v7 → v8
- § «Использование в AI-чатах» переписан — раньше описывал только Path B
  (Self-ID + worker briefing), теперь оба пути с ссылками на 12_ai_workflow

### 00_master_index.md (без v bump)
- Строка «Last updated: 2026-04-18» → «2026-04-19» — sync человеческой метки
  с header-датой v9. Косметика, v не бампаем (правило из § bump).

### VERSIONS.md v10 → v11
- Bump строк для README (8), SYNC_STATE (10), 12_ai_workflow (9), CHANGELOG (12)

### CHANGELOG.md v11 → v12
- Одна строка сверху. 11 записей всего, до потолка 15 далеко.

## Файлы НЕ затронуты
Бизнес-контент (01-11, 99_invariants), все артефакты (.py, .txt), commit_worker_delivery.sh,
00_source_files_index.md, 02_makecom_bot.md, 08_current_state_snapshot.md.
